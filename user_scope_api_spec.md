# Хэрэглэгчийн хамрах хүрээ — API тодорхойлолт
ddd# Хэрэглэгчийн хамрах хүрээ — API тодорхойлолт
ddd# Хэрэглэгчийн хамрах хүрээ — API тодорхойлолт
ddd# Хэрэглэгчийн хамрах хүрээ — API тодорхойлолт
ddd# Хэрэглэгчийн хамрах хүрээ — API тодорхойлолт
**(User Scope / 4-level access model — API Specification)**

Version 1.0 · frontend UI бэлэн (`/users`), backend API одоогоор алга

---

## 1. Зорилго

Одоогийн эрхийн систем (Дүр ба эрх) нь "юу хийж болох" (create/read/update/delete) төдийг л зохицуулдаг — "аль сургуулийн/дүүргийн өгөгдөл харах эрхтэй" гэдгийг зохицуулдаггүй. Энэ спек тэр давхаргыг нэмнэ.

## 2. Түвшингийн тойм

| # | Түвшин | Хамрах хүрээ |
|---|---|---|
| 1 | **Админ** | Бүх эрх (одоо байгаа Дүр ба эрх систем, өөрчлөлт хэрэггүй) |
| 2 | **Зөвлөх/Мэргэжилтэн** | Сургуулийн төрөл сонгоод: "ХОН" бол тодорхой сургуулиуд (олон), бусад төрөл бол ганц дүүрэг — тэр хүрээний сургуулийн менежрүүдийг хянана |
| 3 | **Сургуулийн менежер** | Яг нэг сургууль — зөвхөн тэр сургуулийн гишүүдийг бүртгэнэ |
| 4 | **Багш (гишүүн)** | Админ системд нэвтэрдэггүй тул `Хэрэглэгч` биш — зөвхөн `Гишүүд` хэсэгт бүртгэгдэнэ, энд хамаарахгүй |

Түвшин 2, 3-ын "хамрах хүрээ" мэдээллийг хадгалах API одоогоор алга — frontend талд бүрэн локал state дээр ажиллаж байна (хадгалахад алга болно).

---

## 3. Өгөгдлийн бүтэц

### 3.1 `user_scope` — хэрэглэгч тус бүрт нэг мөр (1:1, `user_id` unique)

Зөвхөн "Зөвлөх/Мэргэжилтэн" болон "Сургуулийн менежер" дүртэй хэрэглэгчид ач холбогдолтой. Бусад дүрт мөр байхгүй байж болно.

| Талбар | Төрөл | Тайлбар |
|---|---|---|
| `user_id` | int FK, unique | |
| `school_type` | enum \| null | §4-ийн код, зөвхөн Зөвлөх/Мэргэжилтэнд |
| `district_au2_code` | string \| null | УБ-ын дүүрэг (`au2.au2_code`) — `school_type` нь `rural` **биш** үед |
| `organization_ids` | int[] | Сонгосон сургуулиуд — зөвхөн `school_type = rural` үед хэрэглэгдэнэ |
| `organization_id` | int \| null | Сургуулийн менежерийн харьяалагдах ганц сургууль |
| `updated_at` | datetime | |

> Нэг хүснэгтэд хоёр дүрийн мэдээллийг хадгалсан нь frontend-ийн одоогийн `UserScope` бүтэцтэй яг тохирно (аль дүрд аль талбарууд хэрэглэгдэхийг §7-д тодорхойлсон).

### 3.2 `school_type` кодууд

Frontend-д харагдах бүтэн нэрийг backend-д хадгалахгүй, доорх тогтвортой кодыг ашиглана:

| код | Гарчиг (сонголтын жагсаалтад харагдах бүтэн нэр) |
|---|---|
| `general` | Ерөнхий боловсролын сургууль /ЕБС/ |
| `preschool` | Сургуулийн өмнөх боловсрол /СӨБ/ |
| `higher` | Дээд боловсрол /ДБ/ |
| `vocational` | Мэргэжлийн боловсрол /МБ/ |
| `science` | Шинжлэх ухааны байгууллага /ШУБ/ |
| `rural` | Хөдөө, орон нутгийн боловсролын байгууллагууд /ХОН/ |

---

## 4. Endpoints

| Method | Path | Тайлбар |
|---|---|---|
| GET | `/api/user/{id}/scope` | Тухайн хэрэглэгчийн хамрах хүрээг буцаана. Мөр байхгүй бол `null` эсвэл хоосон объект. |
| PUT | `/api/user/{id}/scope` | Бүхэлд нь дарж хадгална (upsert — мөр байхгүй бол шинээр үүсгэнэ). |
| DELETE | `/api/user/{id}/scope` | Хамрах хүрээг устгана (дүр өөрчлөгдөж scope шаардлагагүй болоход хэрэгтэй, §8). |

> **Санал болгож буй сайжруулалт:** `GET /api/user` (жагсаалт) хариунд `scope`-ийг шууд оруулж өгвөл (доор §6-ийн жишээтэй ижил) хэрэглэгч бүрээр тусад нь `/scope` дуудах шаардлагагүй болно — таны `/api/member` жагсаалт `position_name`, `profession_name`, `school_category_short_name` зэргийг ийнхүү шууд оруулдагтай ижил хэв маяг.

---

## 5. Жишээ payload-ууд

### 5.1 Зөвлөх/Мэргэжилтэн — "ХОН" (тодорхой сургуулиуд сонгосон)

```json
PUT /api/user/42/scope
{
  "school_type": "rural",
  "district_au2_code": null,
  "organization_ids": [1, 7, 12],
  "organization_id": null
}
```

### 5.2 Зөвлөх/Мэргэжилтэн — бусад төрөл (нэг дүүрэг)

```json
PUT /api/user/42/scope
{
  "school_type": "general",
  "district_au2_code": "01110",
  "organization_ids": [],
  "organization_id": null
}
```

### 5.3 Сургуулийн менежер

```json
PUT /api/user/43/scope
{
  "school_type": null,
  "district_au2_code": null,
  "organization_ids": [],
  "organization_id": 5
}
```

### 5.4 GET /api/user жагсаалтад embed хийсэн тохиолдол (санал)

```json
[
  {
    "id": 42, "username": "99887766", "full_name": "Б. Батбаяр", "email": null,
    "role_id": 108, "role_name": "Зөвлөх/Мэргэжилтэн", "is_active": 1,
    "scope": { "school_type": "rural", "district_au2_code": null, "organization_ids": [1, 7, 12], "organization_id": null }
  }
]
```

---

## 6. Валидаци, бизнес дүрэм

- `school_type = "rural"` үед: `organization_ids` дор хаяж 1 элементтэй байхыг зөвлөж байна (frontend талд одоогоор заавал шаардаагүй ч, "Бүгдийг сонгох" товч байгаа тул хоосон илгээх боломжтой хэвээр).
- `school_type` бусад утгатай (`rural`-аас өөр) үед: `district_au2_code` заавал, `organization_ids` хоосон байх ёстой.
- `organization_id` (Сургуулийн менежерийн талбар) зөвхөн тухайн хэрэглэгчийн дүр "Сургуулийн менежер" төрлийнх үед утгатай байна.
- **Заавал биш, гэхдээ санал болгоно:** backend талд `user_scope`-ийг хадгалахдаа тухайн хэрэглэгчийн `role_id`-той нийцэж байгаа эсэхийг шалгах (жишээ нь admin-д scope бичихийг зөвшөөрөхгүй) — энэ нь дата бохирдохоос сэргийлнэ.
- Хэрэглэгчийн дүр Зөвлөх/Мэргэжилтэн эсвэл Сургуулийн менежерээс өөр болж өөрчлөгдвөл, хуучин `user_scope` мөрийг цэвэрлэхийг зөвлөж байна (`PUT /api/user/{id}` дотор дүр солигдоход автоматаар `DELETE /scope` дуудах, эсвэл хэрэглэгчийн профайл шинэчлэх endpoint дотор нэгтгэх).

---

## 7. Permission кодууд (санал)

Тусад нь permission нэмэхгүй, одоо байгаа `user.read` / `user.update`-той хамт ажиллуулж болно (Хэрэглэгчийн профайлын нэг хэсэг гэж үзвэл). Хэрэв илүү нарийн хяналт хэрэгтэй бол:

```
user_scope.read
user_scope.update
```

---

## 8. Хандалтын хязгаарлалт хэрхэн ажиллах ёстой (дараагийн шат)

Энэ спек зөвхөн `user_scope`-ийг хадгалах CRUD-ыг тодорхойлж байгаа ч, **энэ мэдээллийн жинхэнэ ач холбогдол нь бусад endpoint-ын хариуг хязгаарлахад** оршино:

- **Зөвлөх/Мэргэжилтэн** нэвтрэхэд, `GET /api/member`, `GET /api/organization` зэрэг жагсаалтууд түүний `user_scope`-д тохирсон байгууллага/дүүргийн өгөгдлөөр л шүүгдэж харагдах ёстой:
  - `school_type ≠ rural` бол: `organization.au2_code = district_au2_code`-той тохирох мөрүүд.
  - `school_type = rural` бол: `organization.id IN organization_ids`-д багтах мөрүүд.
- **Сургуулийн менежер** нэвтрэхэд, жагсаалтууд зөвхөн `organization_id = user_scope.organization_id`-той тохирох мөрүүдийг буцаах ёстой.
- Энэ шүүлтийг frontend талд биш, **backend талд (session/token-оос уншиж) хэрэгжүүлэх ёстой** — эс тэгвээс хэрэглэгч API-г шууд дуудаж бусад сургуулийн өгөгдлийг харах боломжтой болно.

Энэ хэсэг тусдаа ажил (жагсаалт бүрийн middleware/query-д нэмэлт нөхцөл оруулах) тул `user_scope` CRUD бэлэн болсны дараа хийхийг зөвлөж байна.

---

## 9. Backend-д анхаарах зүйлс

- `organization_ids`-ийг JSON багана байдлаар хадгалж болно (жижиг массив, тусад нь холбоос хүснэгт хэрэггүй) — таны `menu.blocks`-д ашигласантай ижил хандлага.
- `au1`/`au2` кодуудыг шалгахдаа одоо байгаа `/api/au1`, `/api/au2` эсрэг validate хийж болно (гадаад түлхүүр биш ч, буруу код өгөхөөс сэргийлнэ).
- §6-ын "дүр солигдоход scope цэвэрлэх" дүрмийг хэрэгжүүлэхгүй бол хуучин мэдээлэл үлдэж, дараа дүр дахин Зөвлөх болоход буруу хамрах хүрээ харагдаж магадгүй тул анхаарах хэрэгтэй.
